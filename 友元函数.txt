友元函数：指某些虽然不是类的成员却能够访问类的所有成员的函数
为了使其他类的成员函数来访问该类的私有变量
可以用于运算符重载和两个类共享数据
不是类的成员函数，调用时不通过对象
可以在类内声明，类外定义，可放在类的私有段或公有段，放在私有段和公有段无区别。
	相同点：对类的存取方式相同，可以直接存取类的任何存取控制属性的成员
? ? ? 可以通过对象存取形参、函数体中该类类型对象的所有成员
	不同点：成员函数有this指针，友元函数没有，友元函数不能被继承
